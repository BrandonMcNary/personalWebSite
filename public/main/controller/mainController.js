// mostly the user login function, which could be split out into factories
// other functions can be split out
angular.module('main').controller('mainController', ['$scope', function($scope){
console.log('within main controller')
}]);
